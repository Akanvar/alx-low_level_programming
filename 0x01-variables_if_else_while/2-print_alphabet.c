#include <stdio.h>
/**
 * main - Program that prints alphabets in lowercase
 * Return: always 0 (Success)
 */
int main(void)
{
	char alphabet;

	alphabet = 'a';
	while
		(alphabet<= 'z') {
			putchar(alphabet);
			alphabet++;
		}
	putchar('\n');
	return (0);
}

